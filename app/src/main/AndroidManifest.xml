<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SET_ALARM" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HealthCare"
        tools:targetApi="31">

        <!-- FileProvider for PDF sharing -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>

        <receiver
            android:name=".WaterReminderReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="WATER_REMINDER_ALARM" />
            </intent-filter>
        </receiver>

        <activity
            android:name="com.beast.healthcare.MainActivity"
            android:exported="false" />
        <activity
            android:name="com.beast.healthcare.activities.BMRActivity"
            android:exported="false" />
        <activity
            android:name="com.beast.healthcare.activities.MeditationActivity"
            android:exported="false" />
        <activity
            android:name="com.beast.healthcare.activities.DiseaseTrackerActivity"
            android:exported="false" />
        <activity
            android:name="com.beast.healthcare.activities.MedicationReminderActivity"
            android:exported="false" />
        <activity
            android:name="com.beast.healthcare.activities.NutritionGuideActivity"
            android:exported="false" />
        <activity
            android:name="com.beast.healthcare.activities.YogaActivity"
            android:exported="false" />
        <activity
            android:name="com.beast.healthcare.User.Create_account_activity"
            android:exported="false" />
        <activity
            android:name="com.beast.healthcare.activities.WaterReminderActivity"
            android:exported="false" />
        <activity
            android:name="com.beast.healthcare.User.Login_Activity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>